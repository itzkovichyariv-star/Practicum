# אפיון: הפרדת מועמדים מסטודנטים + דף מועמדים עשיר

**מסמך אפיון | מערכת לניהול הרצאות ופרקטיקום | גרסה 1.2**
מתייחס ל-`index.html` (קובץ יחיד, v2026.04.22)

> ## ⛔ סטטוס: אפיון בלבד — אין לבצע כרגע
>
> **הוחלט על ידי בעל המערכת שלא לבצע שינויים בשלב זה, כדי לא לשבור קישורים קיימים או התנהגות קיימת.**
>
> `index.html` **לא שונה**. המסמך מתעד ניתוח והצעה בלבד.
>
> ההחלטה מבוססת: פרק 3.2.1 מראה ששינוי שמות הקבצים או מסלול הקישור **ישבור קישורי שיתוף שכבר נשלחו לארגונים**. אין לגעת בשמות קבצים או בבניית הקישורים בלי החלטה מפורשת ובלי מיפוי מוקדם של הקישורים שכבר בחוץ.
>
> **המחיר של ההמתנה, לתשומת לב:** כל עוד שני שלבי הקו״ח כותבים לאותו שם קובץ (סעיף 2.4), קו״ח המועמדות ממשיכים להידרס בכל פעם שסטודנט מעלה קו״ח מעודכנים. מדובר באובדן ארכיון, לא בשיבוש תפעולי — השליחה לארגון ממשיכה לעבוד כרגיל.
>
> חלקים שאינם נוגעים לקבצים ולקישורים (הפרדת מועמדים מסטודנטים, העברת סיכומי הראיון וההערכות, עושר התצוגה) ניתנים לביצוע בנפרד וללא סיכון לקישורים. ראה פרק 9.

> **שינויי גרסה 1.2** — שני תיקונים בעקבות הערות בעל המערכת:
> 1. לא ניתן להגיש מועמדות ללא קורות חיים, ולכן «CV חסר» אינו התרעה רלוונטית בדף המועמדים. תפקיד הצ׳יפ הוא גישה, לא התרעה.
> 2. הסטודנט מעלה **קורות חיים מעודכנים** אחרי ההכנה ולפני בחירת מקומות התנסות, והם שנשלחים לארגון — **כקישור, לא כקובץ מצורף**.
>
> התווסף פרק 3 הממפה את שלבי הסטודנט ומודל שני מסלולי הקו״ח. נמצא ליקוי נוסף: **כל קישור לקובץ במערכת מצביע על תיקייה משותפת ולא על קובץ** (סעיף 2.5), ולכן קישור שנשלח לארגון חושף את קורות החיים של כל הסטודנטים בקורס. תוקן גם ניתוח שגוי מגרסה 1.0 לגבי שורת הסטודנט (סעיף 2.5.1).

---

## 1. תקציר מנהלים

| # | נושא | מהות |
|---|------|------|
| **א** | **הפרדת אוכלוסיות** | ברגע שמועמד עבר ראיון הוא הופך לסטודנט ויורד מדף המועמדים. תיק המועמדות (טופס הגשה, קו״ח למועמדות, סיכום ראיון, ציוני הערכה) עובר איתו ונגיש מדף הסטודנט. רשומת המועמדות **נשמרת בארכיון** ולא נמחקת. |
| **ב** | **דף מועמדים עשיר** | שדרוג התצוגה בדף המועמדים לרמת העושר של דף הסטודנטים, כך שטופס המועמדות, קורות החיים, תאריך הראיון והציון נגישים ישירות מהשורה בלי להיכנס לכרטיס. |

שני השינויים תלויים זה בזה: ברגע שהמועמדים «האמיתיים» הם היחידים ברשימה, יש מקום להציג עליהם יותר מידע.

---

## 2. מצב קיים — ניתוח מדויק

### 2.1 מודל הנתונים

**`DB.candidates[]`:**

```
id, name, phone, email, city, courseId, year
applicationDate, preferredArea, notes
file_cv          → {name, originalName, size, uploadDate, folder}
file_application → {name, originalName, size, uploadDate, folder}
interviewDate, interviewResult ('passed'|'failed'|''), interviewSummary
evalCommitment, evalMotivation, evalCommunication, evalEnglish, evalAcquaintance, evalScore
rejectionReason, acceptedDate, manualSummary
status  ← מחושב אוטומטית
```

**`DB.students[]`:**

```
id, name, phone, email, city, courseId, year
prepDone, prepDate
acceptedOrg, interviews, hired, preferences[3], submitted[]
hoursReported, hoursApproved, hoursConfirmed, orgFeedback
file_cv, file_form, file_summary, file_feedback
cvUrl, formUrl, summaryUrl
notes, manualSummary, fromCandidate
```

### 2.2 חישוב הסטטוס — `computeCandidateStatus()` (index.html:4373)

```
interviewResult === 'failed'                    → 'rejected'
interviewResult === 'passed'                    → 'accepted'   ← הנקודה הקריטית
requireInterview && interviewSummary && date    → 'interviewed'
requireInterview && interviewDate               → 'interview_scheduled'
CV + טופס קיימים (לפי דרישות הקורס)             → 'docs_ready'
אחרת                                            → 'pending'
```

`accepted` אינו מצב סופי בתהליך — הוא אמור להימשך שניות, עד ההעברה לסטודנטים. בפועל הוא נשאר לתמיד, עם תג ירוק «התקבל» + תג ירוק «עבר ראיון» (index.html:4067, 4071).

### 2.3 ההעברה לסטודנטים — `transferToStudents()` (index.html:4709)

הפעולה ידנית, לחיצה אחת לכל מועמד, ולא קורית שום דבר אם לא לוחצים.

| מה עובר | מה **הולך לאיבוד** |
|---------|---------------------|
| `name, phone, email, city` | `interviewSummary` — **סיכום הראיון** |
| `file_cv → file_cv` | `interviewDate`, `interviewResult` |
| `file_application → file_form` | `evalCommitment / Motivation / Communication / English / Acquaintance` |
| `notes` + שורת «הועבר בתאריך» | `evalScore` — הציון הממוצע |
| `courseId, year` | `applicationDate`, `acceptedDate`, `preferredArea` |
| `fromCandidate: true` | `manualSummary`, `rejectionReason`, ה-`id` המקורי |

בסוף הפונקציה: `DB.candidates = DB.candidates.filter(x => x.id !== candId)` — **הרשומה נמחקת לגמרי**.

**המשמעות:** התהליך הקיים לא רק לא מעביר את המידע — הוא גם מוחק את ההיסטוריה. אחרי העברה, המועמד נעלם מהמשפך בדשבורד (index.html:3892), מדוח המועמדים (index.html:5887), מייצוא האקסל (index.html:6529), ומלוח השנה (index.html:2918 — הראיון שלו נעלם מהלוח). לכן **הפתרון חייב להיות ארכוב ולא מחיקה.**

### 2.4 באג קיים — קו״ח המועמדות וקו״ח הסטודנט כותבים לאותו קובץ

שתי פונקציות ההעלאה בונות את שם הקובץ מאותו תבנית, ושולחות אותו לאותה תיקייה:

| | פונקציה | שם הקובץ | תיקייה |
|---|---|---|---|
| מועמד | `uploadCandidateFile()` — index.html:4137-4139 | `דנה_כהן_CV.pdf` | `קורות_חיים` |
| סטודנט | `uploadStudentFile()` — index.html:6630-6632 | `דנה_כהן_CV.pdf` | `קורות_חיים` |

`FILE_FOLDERS.cv = 'קורות_חיים'` (index.html:4157) משותף לשניהם. כלומר **כשהסטודנט מעלה קו״ח מעודכנים אחרי ההכנה, הוא כותב בדיוק על שם הקובץ של קו״ח המועמדות.** בדפדפן שתומך ב-`showSaveFilePicker` תוצג שאלת החלפה; בנתיב ההורדה החלופי הקובץ נשמר עם אותו שם וייתקל בהתנגשות ברגע שיועבר לתיקייה. הדריסה אינה שקטה, אבל מבנה השמות מזמין אותה.

במקביל, במודל הנתונים: `saveStudent()` (index.html:5001-5011) מציב את ההעלאה החדשה ב-`file_cv` **במקום** הישנה. אין שמירה של הגרסה הקודמת.

> ההעברה הקיימת (index.html:4736) מכניסה את קו״ח המועמדות בדיוק אל `student.file_cv` — כלומר אל המשבצת שתידרס בהמשך. כל אפיון שמעביר קו״ח מועמדות אל `student.file_cv` מנציח את הבאג.

### 2.5 שליחת הקו״ח לארגון — קישור, ולעולם לא לקובץ

הקו״ח נשלחים לארגון כ**קישור** ולא כקובץ מצורף. אף מסלול יציאה במערכת אינו מצרף קבצים: `sendGroupEmail()` (5623) בונה `mailto:` עם טקסט בלבד, אף אחת מ-11 תבניות המייל (5478-5490) אינה מזכירה קו״ח, וגם `generatePlacementLetter()` (3438) לא. מה שנשלח הוא הקישור שנבנה במערכת.

**וכאן הליקוי המרכזי: כל קישור לקובץ במערכת מצביע על תיקייה, לא על קובץ.**

```
uploadStudentFile()  שומר:  folder = 'תשפ״ו/פרקטיקום_משאבי_אנוש/קורות_חיים'
                            name   = 'דנה_כהן_CV.pdf'

getDataFileFolderUrl(folder) בונה:
   .../onedrive.aspx?id=<...>/data/תשפ״ו/פרקטיקום_משאבי_אנוש/קורות_חיים
                                                              ↑ נעצר כאן
```

`file_cv.name` נשמר — אבל **לעולם אינו מצורף לכתובת**. בשורת הסטודנט (index.html:4846) הוא משמש אך ורק כ-`title`, כלומר טולטיפ. הקישור עצמו נעצר ברמת התיקייה.

**המשמעות:** התיקייה `קורות_חיים` היא תיקייה אחת לכל הקורס והשנה, ובה קורות החיים של **כל** הסטודנטים. קישור שנשלח לארגון פותח את כולה.

| בעיה | חומרה |
|---|---|
| **הארגון מקבל גישה לקורות החיים של כל הסטודנטים בקורס** | חשיפת מידע אישי של צד שלישי |
| הארגון צריך לאתר בעצמו את הקובץ הנכון מתוך הרשימה | תפעולית |
| בתיקייה יש קובץ אחד בשם `דנה_כהן_CV.pdf` ואין בשמו דבר שמעיד איזו גרסה הוא | ראה 2.4 |

### 2.5.1 תיקון לניתוח קודם

בגרסה 1.0 של מסמך זה נכתב ששורת הסטודנט «פותרת נכון» את הקישור לקובץ הספציפי. **זה לא מדויק.** שורת הסטודנט וגם שורת המועמד מגיעות לאותה תיקייה משותפת. ההבדל היחיד ביניהן הוא ששורת הסטודנט בודקת אם קיימת רשומת קובץ ומשקפת זאת, בעוד שורת המועמד מציגה את הקישור תמיד. **אף אחת מהן אינה מובילה לקובץ.**

### 2.5.2 מה כן קיים — שדה הקישור הידני

`student.cvUrl` («קישור קורות חיים», index.html:1035) קיים בדיוק למטרה הזו, ו-`getFileUrl()` (index.html:2871) מעדיף אותו כשהוא מתחיל ב-`http`. זהו המקום הנכון לשמור **קישור שיתוף אמיתי** שנוצר ב-SharePoint. היום השדה מוסתר מאחורי `<details>` ואינו חלק מהתהליך.

### 2.6 השורה בדף המועמדים — `renderCandidates()` (index.html:4023)

```
[שם]                                    [סיכום תיק] [ערוך] [מחק]
[טלפון • מייל • עיר]
[תג סטטוס] [CV] [טופס מועמדות] [ראיון: תאריך] [עבר ראיון]
[הערות]
```

שורות 4068-4069 מפנות ל-`getDataFolderUrl('cv')` ו-`getDataFolderUrl('application')` — **תיקיית הקורס המשותפת**, אותו קישור בדיוק לכל מועמד, ומוצג גם כשאין לו רשומת קובץ כלל. כפי שהובהר ב-2.5, גם שורת הסטודנט מגיעה לאותה תיקייה; ההבדל היחיד הוא שהיא לפחות משקפת אם קיימת רשומת קובץ.

**הבהרה חשובה (תיקון גרסה 1.1):** אי אפשר להגיש מועמדות ללא קורות חיים — הדרישה נאכפת מוקדם יותר בתהליך, מחוץ למערכת. לכן «CV חסר» **אינו** התרעה תפעולית בדף המועמדים. הליקוי האמיתי בצ׳יפים האלה הוא שהם **לא מובילים לקובץ הנכון**: תפקידם גישה, לא התרעה.

### 2.7 פערי תצוגה — מועמד מול סטודנט

| רכיב | דף סטודנטים | דף מועמדים |
|------|:-----------:|:----------:|
| Checkbox + פעולות מרובות | ✔ | ✘ |
| כפתור מייל מהיר | ✔ | ✘ |
| קישור לקובץ הספציפי | ✔ | ✘ (תיקייה כללית) |
| סימון «לא רלוונטי» | ✔ | ✘ |
| תגי סטטוס עשירים | ✔ (5 סוגים) | חלקי |
| שורות מידע נוספות | ✔ (העדפות, הגשות) | ✘ |
| מיון | ✘ | ✘ |
| מסנן לפי מצב מסמכים | ✘ | ✘ |

---

## 3. מחזור חיי הסטודנט — מה אסור לדרוס

התהליך אינו נגמר בהמרה. אלה השלבים שאחריה, כפי שהם מיוצגים בקוד היום:

| # | שלב | שדות | שער קיים |
|---|-----|------|----------|
| 1 | **הכנה לפרקטיקום** | `prepDone`, `prepDate` | — |
| 2 | **העלאת קו״ח מעודכנים** | *אינו מיוצג היום* | — |
| 3 | **בחירת 3 העדפות** | `preferences[3]` | — |
| 4 | **הגשה למעסיקים** | `submitted[]` | — |
| 5 | **ראיונות אצל מעסיקים** | `interviews` (מונה) | — |
| 6 | **התקבל לארגון** | `acceptedOrg` | ✔ חסום ללא `prepDone` (index.html:4971) |
| 7 | **שעות התנסות** | `hoursReported`, `hoursApproved`, `hoursConfirmed` | — |
| 8 | **חוות דעת ארגון** | `orgFeedback`, `file_feedback` | — |
| 9 | **נקלט לעבודה** | `hired` | — |

שלב 2 הוא היחיד שאין לו ייצוג במערכת, והוא בדיוק זה שמתנגש עם ההמרה. תבנית המייל «הזמנה להכנה» (index.html:5479) כבר מציינת «כתיבת קורות חיים» כאחד מנושאי הסדנה — כלומר התהליך קיים בשטח, רק לא במודל.

### 3.1 בדיקת התנגשות — כל שלב מול האפיון

| שדה בשלבים הבאים | האם האפיון נוגע בו | מסקנה |
|---|---|---|
| `prepDone`, `prepDate` | לא | בטוח |
| **`file_cv`** | **כן — ההמרה כותבת אליו** | ⚠ **התנגשות. נפתר ב-3.2** |
| `preferences[]`, `submitted[]` | לא | בטוח |
| `interviews` | לא — ראיון הקבלה נשמר ב-`candidacy.interviewDate` | בטוח, וזו הסיבה לקינון |
| `acceptedOrg`, `hired` | לא | בטוח |
| שעות וחוות דעת | לא | בטוח |
| `file_form` | כן — מקבל את טופס המועמדות | בטוח: זו בדיוק משמעותו במודאל הסטודנט (index.html:1014), ואין טופס מאוחר יותר שמשתמש במשבצת |
| `file_summary` | לא — סיכום הראיון נשמר כטקסט ב-`candidacy` | בטוח; המשבצת נשארת פנויה לסריקה |

> `student.interviews` = מספר הראיונות **אצל מעסיקים**. `candidacy.interviewDate` = ראיון **הקבלה לפרקטיקום**. שני דברים שונים לגמרי, שקל לבלבל ביניהם — הקינון תחת `candidacy` מבטיח שלא יתערבבו.

### 3.2 מודל שני מסלולי קורות חיים

| | קו״ח מועמדות | קו״ח מעודכנים |
|---|---|---|
| **מתי** | לפני הגשת המועמדות | אחרי ההכנה, לפני בחירת מקומות התנסות |
| **איפה** | `candidacy.file_cv` | `student.file_cv` |
| **מצב** | קפוא, קריאה בלבד, חלק מתיק המועמדות | תפעולי, נערך, זה שנשלח למעסיקים |
| **שם הקובץ** | `דנה_כהן_CV_מועמדות.pdf` | `דנה_כהן_CV_מעודכן.pdf` |

**מה ההמרה עושה:** מעתיקה את קו״ח המועמדות אל `candidacy.file_cv`, **וגם** מציבה אותם ב-`student.file_cv` עם סימון `stage: 'candidacy'`. כך לא נוצר מצב שבו לסטודנט אין קו״ח כלל, והמצב מוצג ביושר.

**מה קורה בהעלאה מאוחרת:** העלאת קו״ח בכרטיס הסטודנט כותבת ל-`student.file_cv` עם `stage: 'updated'` + `cvUpdatedDate`. `candidacy.file_cv` **אינו נגזר ואינו נדרס** — הוא נשאר כפי שהיה.

**שדה חדש אחד בלבד:** `student.file_cv.stage` בעל שני ערכים, `'candidacy'` או `'updated'`. ממנו נגזרים הצ׳יפ, השער וההתרעה.

**תיקון השמות** ב-`uploadCandidateFile()` ו-`uploadStudentFile()` כך ששני השלבים לא יכתבו לאותו קובץ. התיקון חל על העלאות חדשות בלבד — קבצים שכבר נדרסו בעבר אינם ניתנים לשחזור בקוד.

#### כלל הדריסה — מה מותר לדרוס ומה לא

לא כל דריסה היא באג. ההבחנה:

| קובץ | נדרס? | נימוק |
|---|---|---|
| `CV_מועמדות` | **לעולם לא** | מסמך ארכיוני. נכתב פעם אחת בהמרה, וזהו |
| `CV_מעודכן` | **כן, בעדכון מאוחר יותר** | **רצוי** — הארגון צריך תמיד לקבל את הגרסה האחרונה. אם הסטודנט מעלה גרסה משופרת, היא מחליפה את הקודמת |

זו התשובה לשאלה «אז אולי יש דריסה של הישנים»: הדריסה **בתוך** מסלול הקו״ח המעודכנים היא התנהגות נכונה ורצויה. מה שאסור הוא שהמסלול המעודכן ידרוס את **מסלול המועמדות** — וזה בדיוק מה שקורה היום, כי שני המסלולים חולקים שם קובץ אחד.

תוצאה מעשית: אחרי התיקון, בתיקיית `קורות_חיים` יהיו לכל סטודנט **שני קבצים בעלי שמות מובחנים**.

#### אזהרה: שמות מובחנים לבדם שוברים קישורים קיימים

שינוי שם הקובץ אינו ניטרלי כשמה שנשלח לארגון הוא **קישור**:

| | היום (שם זהה) | אחרי שינוי השם בלבד |
|---|---|---|
| קישור שנשלח לארגון | ממשיך לעבוד, ומגיש תמיד את הקובץ האחרון שנכתב | **מצביע על הקובץ הישן** — הארגון מקבל את קו״ח המועמדות |
| ארכיון המועמדות | נדרס ואבד | נשמר |

כלומר הדריסה של היום «עובדת» בשליחה בדיוק בגלל שהיא הורסת את הארכיון. **אי אפשר לתקן רק את השמות** — התיקון חייב לכלול גם את מסלול הקישור, אחרת מוחלף באג של אובדן נתונים בבאג של קישור מיושן.

**כלל מחייב:** ברגע ש-`file_cv.stage` הופך ל-`'updated'`, כל קישור שמור ב-`student.cvUrl` שנוצר עבור הגרסה הקודמת מסומן כמיושן ומוצג בשורה כ**«הקישור מצביע על גרסה קודמת — יש לרענן»**, עד שהוחלף.

### 3.2.1 קישור ברמת הקובץ, לא ברמת התיקייה

זו דרישה נפרדת ומחייבת, שנובעת מסעיף 2.5:

| רכיב | דרישה |
|---|---|
| `getDataFileUrl(fileObj)` — **פונקציה חדשה** | בונה כתובת הכוללת את שם הקובץ: `<root>/data/<folder>/<name>`, במקום להיעצר בתיקייה |
| כל צ׳יפ קובץ | מפנה לקובץ עצמו. פתיחה של תיקייה נשארת רק ככפתור נפרד «פתח תיקייה» |
| שליחה לארגון | **לעולם לא קישור תיקייה** — קישור תיקייה חושף את קורות החיים של כל הסטודנטים בקורס |

**מגבלה שיש להכיר:** כתובת נתיב שנבנית במחרוזת פותחת את הקובץ רק למי שכבר יש לו הרשאה בתיקייה. **ארגון חיצוני לא יוכל לפתוח אותה.** קישור שיתוף חיצוני נוצר על ידי SharePoint עצמו («שתף» → «כל מי שיש לו הקישור»), והמערכת אינה יכולה לייצר אותו בבניית מחרוזת — לכך נדרשת אינטגרציה מול Graph API, שאינה קיימת.

**לכן התהליך הנכון, בלי אינטגרציה:**

1. הצ׳יפ בשורה מוביל **ישירות לקובץ** — לחיצה אחת, בלי לחפש בתיקייה.
2. שם, תפריט «שתף» של SharePoint מייצר קישור שיתוף חיצוני.
3. הקישור מודבק בשדה `student.cvUrl` — כפתור **«הדבק קישור שיתוף»** בשורה, במקום השדה המוסתר של היום.
4. משם הקישור הוא שנשלח לארגון, והוא מסומן במפורש כשייך לגרסה המעודכנת.
5. בעדכון קו״ח — הקישור מסומן כמיושן עד שיוחלף.

בכך המערכת מקצרת את התהליך ככל שניתן, ומסמנת במפורש את שני המצבים המסוכנים: קישור תיקייה, וקישור שאינו תואם לגרסה הנוכחית.

### 3.3 שער קו״ח מעודכנים

במקביל לשער ההכנה הקיים (`acceptedOrg` חסום כל עוד `!prepDone`):

| מיקום | התנהגות |
|---|---|
| כרטיס הסטודנט | אזהרה רכה ליד ההעדפות: «טרם הועלו קו״ח מעודכנים לאחר ההכנה» — **מזהיר, לא חוסם**, כדי לא לשבור הזנת נתונים היסטוריים |
| שורת הסטודנט | צ׳יפ כתום «קו״ח מועמדות» עד להחלפה, ואז ירוק «קו״ח מעודכנים · 12/03/26» |
| מסנן בדף הסטודנטים | «עברו הכנה · טרם העלו קו״ח מעודכנים» |
| התראות דשבורד | «N סטודנטים עברו הכנה וטרם העלו קו״ח מעודכנים» |

בכרטיס הסטודנט, תיבת «קורות חיים» מתפצלת לשתי משבצות: **קו״ח מועמדות** (קריאה בלבד, מתוך תיק המועמדות) ו**קו״ח מעודכנים** (העלאה).

**הגנה בנקודת השליחה לארגון.** מה שנשלח הוא קישור (סעיף 2.5), ולכן הצ׳יפ בשורת הסטודנט הוא ההתראה האחרונה לפני שהוא יוצא החוצה. הוא נושא את התאריך — `קו״ח מעודכנים · 12/03/26` — וכשהוא כתום, אין לשלוח.

**שלושה מצבי אזהרה בשורת הסטודנט:**

| מצב | תצוגה |
|---|---|
| `stage = 'candidacy'` | 🟠 «קו״ח מועמדות» — אין לשלוח לארגון |
| `stage = 'updated'` + `cvUrl` תואם | 🟢 «קו״ח מעודכנים · 12/03/26» + «העתק קישור» |
| `stage = 'updated'` + `cvUrl` מגרסה קודמת | 🔴 «הקישור מצביע על גרסה קודמת — יש לרענן» |

כפתור **«העתק קישור לקו״ח מעודכנים»** מעתיק את `cvUrl` כשהוא קיים ותואם. כשאין קישור שיתוף — הכפתור מוביל ישירות לקובץ ב-SharePoint כדי לייצר אותו שם.

---

## 4. עקרונות מנחים

1. **מקור אמת יחיד.** אחרי ההמרה, כרטיס הסטודנט הוא היחיד שנערך. רשומת המועמדות הופכת לקריאה-בלבד.
2. **שום נתון לא נעלם.** לא מוחקים רשומות ולא דורסים קבצים — מארכבים ומפרידים מסלולים.
3. **המידע במקום שבו מקבלים את ההחלטה.** אם צריך לפתוח כרטיס כדי לראות מה נכתב בראיון — התצוגה נכשלה.
4. **התרעה רק היכן שיש שער אמיתי.** «חסר» מוצג רק כשחסרונו עוצר תהליך. אין התרעות על מה שנאכף מוקדם יותר.
5. **הרחבה, לא שכתוב.** כל השדות אדיטיביים; קבצי JSON וגיבויים ישנים ממשיכים להיטען.
6. **הפיכות.** לכל פעולת המרה יש `saveBeforeAction()` וכפתור «החזר למועמדים».

---

## 5. פתרון א' — המרת מועמד לסטודנט

### 5.1 תוספות למודל הנתונים

**על רשומת המועמד:**

| שדה | טיפוס | משמעות |
|-----|-------|---------|
| `converted` | boolean | `true` = הומר לסטודנט; יורד מהרשימה הפעילה |
| `convertedDate` | string | תאריך ההמרה |
| `convertedBy` | string | מי ביצע, מתוך `DB.currentUser` |
| `studentId` | number | מזהה הסטודנט שנוצר |

**על רשומת הסטודנט:**

| שדה | טיפוס | משמעות |
|-----|-------|---------|
| `candidateId` | number | מזהה רשומת המועמדות המקורית |
| `candidacy` | object | **תיק המועמדות המלא** |
| `file_cv.stage` | `'candidacy'` \| `'updated'` | באיזה שלב הקו״ח שבמשבצת התפעולית |
| `cvUpdatedDate` | string | מתי הועלו קו״ח מעודכנים |

```js
candidacy: {
  candidateId:      12,
  applicationDate:  '2026-01-14',
  interviewDate:    '2026-02-03',
  interviewResult:  'passed',
  interviewSummary: 'מוטיבציה גבוהה, ניסיון קודם בגיוס...',
  preferredArea:    'גיוס ומיון',
  acceptedDate:     '2026-02-03',
  evalScore:        88,
  evals: {
    commitment: 'גבוה', motivation: 'גבוה',
    communication: 'בינוני', english: 'גבוה', acquaintance: 'נמוך'
  },
  file_cv:          {name:'דנה_כהן_CV_מועמדות.pdf', folder:'...'},   // קפוא
  manualSummary:    '',
  convertedDate:    '2026-02-03',
  convertedBy:      'ד"ר יריב איצקוביץ'
}
```

**נימוק לאובייקט מקונן:** מונע התנגשות שמות עם שדות הסטודנט הקיימים — ל-`student.interviews` כבר יש משמעות אחרת לגמרי — שומר על הכרטיס נקי, ומאפשר תצוגה גנרית של הסקשן.

### 5.2 מתי מתרחשת ההמרה

ב-`saveCandidate()` (index.html:4389), אחרי חישוב הסטטוס:

```
אם status הפך ל-'accepted' והמועמד עדיין לא converted:
  → "‏{שם} עבר/ה את הראיון. להעביר עכשיו לרשימת הסטודנטים?
     תיק המועמדות — טופס ההגשה, קו״ח וסיכום הראיון — יעבור לכרטיס הסטודנט."
     [כן, העבר] (ברירת מחדל)   [לא כרגע]
  → "כן"  : convertCandidateToStudent(id) + טוסט עם קישור "פתח כרטיס סטודנט"
  → "לא"  : נשאר בדף המועמדים בסטטוס "התקבל — ממתין להעברה"
```

**מנגנון השלמה** — באנר קבוע בראש דף המועמדים כשקיימים מועמדים `accepted && !converted`:

> **3 מועמדים עברו ראיון וממתינים להעברה לסטודנטים** &nbsp; [העבר את כולם] [הצג רק אותם]

**הגדרה בקורס:** «העבר אוטומטית לסטודנטים בסיום ראיון מוצלח» (ברירת מחדל: מסומן), יחד עם `requireCV / requireApplication / requireInterview / requirePrep` הקיימות (index.html:1746). כשמסומן — ההמרה קורית בלי דיאלוג, עם טוסט ואפשרות ביטול.

### 5.3 מיפוי מלא — מה עובר בהמרה

| מקור (candidate) | יעד (student) | הערות |
|---|---|---|
| `name, phone, email, city` | אותם שדות | |
| `courseId, year` | אותם שדות | |
| `file_cv` | `candidacy.file_cv` **וגם** `file_cv` עם `stage:'candidacy'` | **שינוי מגרסה 1.0** — העותק הארכיוני קפוא; המשבצת התפעולית מסומנת ותוחלף בקו״ח מעודכנים |
| `file_application` | `file_form` | «טופס הגשת מועמדות» בכרטיס הסטודנט |
| `interviewSummary` | `candidacy.interviewSummary` | **חדש — לא עובר היום** |
| `interviewDate`, `interviewResult` | `candidacy.*` | **חדש** |
| `evalScore` + 5 שדות `eval*` | `candidacy.evalScore`, `candidacy.evals{}` | **חדש** |
| `applicationDate`, `acceptedDate` | `candidacy.*` | **חדש** |
| `preferredArea` | `candidacy.preferredArea` | **חדש** |
| `manualSummary` | `candidacy.manualSummary` | **חדש** |
| `notes` | `notes` | ללא שורת «הועבר בתאריך» — המידע עובר לשדות מובנים |
| `id` | `candidateId` + `candidacy.candidateId` | |
| — | `fromCandidate: true` | תאימות אחורה (index.html:3939, 4843, 5109) |
| — | `prepDone: false`, `interviews: 0`, `hired: false`, `preferences: ['','','']`, `submitted: []` | ערכי פתיחה |

**סיכום הראיון** נשמר כטקסט ב-`candidacy.interviewSummary`. יתווסף כפתור **«ייצא סיכום ראיון ל-Word»** באמצעות `openDocWindow()` הקיים (index.html:3186). משבצת `file_summary` נשארת פנויה לסריקה של סיכום חתום.

### 5.4 מה קורה לרשומת המועמד

**היא נשארת**, מסומנת `converted: true`:

* אינה מוצגת ברשימת המועמדים הפעילה.
* מוצגת בתצוגת **ארכיון** (צ׳יפ «הומרו לסטודנטים» + מסנן) — מעומעמת, עם תג «סטודנט» וכפתור **«פתח כרטיס סטודנט»**.
* עריכה חסומה: `editCandidate()` פותחת קריאה-בלבד עם *«המועמד/ת הומר/ה לסטודנט/ית — יש לערוך בכרטיס הסטודנט»*.
* **«החזר למועמדים»**: מוחקת את הסטודנט (רק אם אין `acceptedOrg`/`submitted`/`hoursReported`), מנקה `converted/studentId`, מחזירה לרשימה.

### 5.5 מיגרציה לנתונים קיימים

רצה פעם אחת ב-`loadFromStorage()` (index.html:1378) ואחרי טעינה מהענן, מוגנת בדגל `DB.settings.candidateMigration === 'v1'`. זמינה גם ידנית מהגדרות: **«סנכרון מועמדים ↔ סטודנטים»**.

```
לכל מועמד c שבו c.converted === undefined:
  1. חפש סטודנט תואם באותו courseId + year לפי סדר:
       א. email זהה (case-insensitive)
       ב. 9 ספרות אחרונות של הטלפון
       ג. שם מנורמל זהה (trim + כיווץ רווחים)
  2. אם נמצא סטודנט s:
       - c.converted = true;  c.studentId = s.id;  c.convertedDate = c.acceptedDate || היום
       - s.candidateId = c.id
       - s.candidacy = מולא מ-c  (רק שדות ריקים — לא דורסים מידע קיים)
       - s.candidacy.file_cv = c.file_cv           ← העותק הארכיוני
       - אם s.file_cv ריק  → s.file_cv = c.file_cv עם stage:'candidacy'
         אם s.file_cv קיים → נשאר כמות שהוא, מסומן stage:'updated'
           (הנחה: סטודנט קיים עם קו״ח כבר עבר את שלב העדכון)
       - s.file_form ||= c.file_application
  3. אם לא נמצא ו-c.status === 'accepted':
       - נשאר מועמד פעיל, מסומן «ממתין להעברה»
  4. אחרת: c.converted = false
```

בסיום: *«סונכרנו 14 מועמדים לסטודנטים קיימים · 3 ממתינים להעברה · 27 מועמדים פעילים»*.
לפני הריצה — `saveBeforeAction()`.

### 5.6 השפעות על מסכים אחרים

| מסך / פונקציה | מיקום | שינוי נדרש |
|---|---|---|
| `getFilteredCandidates()` | 1733 | להוסיף `getActiveCandidates()` (ללא מומרים); הקיימת נשארת «כל ההיסטוריה» |
| כרטיסיית מועמדים בדשבורד | 3862 | ספירת פעילים; tooltip «‎+N הומרו» |
| משפך המועמדים | 3892-3922 | מקטע **«הומרו לסטודנטים»**; חישוב על כל ההיסטוריה לשמירת שיעור המרה נכון |
| התראות דשבורד | 3927 | «N ממתינים להעברה» + «N עברו הכנה וטרם העלו קו״ח מעודכנים» |
| `getCalendarEvents()` | 2918 | ראיונות של מומרים ממשיכים להופיע; `onclick` → `editStudent(studentId)` |
| דוח מועמדים | 5887 | עמודת «הומר לסטודנט» + «שיעור המרה» |
| `exportToExcel('candidates')` | 6529 | עמודות: ציון, הומר, תאריך המרה |
| `exportToExcel('students')` | 6543 | עמודת «שלב קו״ח» |
| `globalSearch()` | 6008 | מומר מסומן «מועמד (הומר)»; לחיצה פותחת את כרטיס הסטודנט |
| `importJSON()` | 3806 | השדות החדשים בייבוא/ייצוא |
| `supabaseSaveData()` | 6804 | **אין שינוי** — ה-snapshot שולח את המערכים במלואם |

### 5.7 מקרי קצה

| תרחיש | התנהגות נדרשת |
|---|---|
| מועמד עבר ראיון וכבר קיים סטודנט באותו שם | לא ליצור כפילות — למזג את תיק המועמדות לסטודנט הקיים, «מוזג לסטודנט קיים» |
| שינוי «עבר ראיון» ל«לא עבר» אחרי המרה | חסום — נדרש קודם «החזר למועמדים» |
| מחיקת סטודנט שהומר | הרשומה במועמדים חוזרת לפעילה + טוסט |
| מועמד שנדחה (`rejected`) | **נשאר בדף המועמדים** — הוא מועמד, לא סטודנט. ניתן להסתירו במסנן |
| קורס ללא ראיון (`requireInterview: false`) | ההמרה ידנית מכפתור, כבר מסטטוס `docs_ready` |
| החלפת שנה/קורס באמצע | ההמרה שומרת `courseId`/`year` מהמועמד, לא מהקונטקסט |
| סטודנט מעלה קו״ח מעודכנים | `candidacy.file_cv` **אינו משתנה**; `student.file_cv` מתעדכן ל-`stage:'updated'` |

---

## 6. פתרון א' — תיק המועמדות בדף הסטודנט

### 6.1 בשורת הסטודנט (index.html:4824)

```
🎓 מועמדות ✓ 03/02/26      ← תג סגול; לחיצה פותחת את סקשן תיק המועמדות
ציון 88                     ← רק אם evalScore קיים (≥85 ירוק, 70-84 כתום, <70 אפור)
קו״ח מועמדות / קו״ח מעודכנים · 12/03/26   ← כתום עד ההחלפה, ירוק אחריה
```

הקישור **«טופס»** הקיים כבר מצביע על `file_form` — יעודכן ה-`title` ל«טופס הגשת מועמדות (הועבר ממועמדות)».
חיפוש הסטודנטים (index.html:4788) יורחב לכלול את `candidacy.interviewSummary`.

### 6.2 בכרטיס הסטודנט

סקשן חדש **«▸ תיק מועמדות»**, מקופל כברירת מחדל, מוצג רק כשקיים `candidacy`, אחרי «פרטים אישיים» ולפני «הכנה»:

```
┌─ 🎓 תיק מועמדות  ─────────────────────  קריאה בלבד ──┐
│ תאריך הגשה   14/01/26      ראיון  03/02/26  ✓ עבר   │
│ תחום מבוקש   גיוס ומיון     ציון ממוצע   88          │
│                                                       │
│ הערכת ראיון                                           │
│ מחויבות גבוה · מוטיבציה גבוה · תקשורת בינוני ·        │
│ אנגלית גבוה · היכרות נמוך                             │
│                                                       │
│ סיכום ראיון                                           │
│ ┌───────────────────────────────────────────────┐    │
│ │ מוטיבציה גבוהה, ניסיון קודם בגיוס...          │    │
│ └───────────────────────────────────────────────┘    │
│                                                       │
│ [📄 קו״ח מועמדות] [📝 טופס מועמדות] [סיכום תיק]      │
│ הומר ב-03/02/26 ע״י ד״ר יריב איצקוביץ                 │
└───────────────────────────────────────────────────────┘
```

תיבת הקבצים הקיימת מתפצלת: **קו״ח מועמדות** (קריאה בלבד, מתוך `candidacy.file_cv`) ו**קו״ח מעודכנים** (העלאה, `student.file_cv`), עם אזהרה רכה ליד ההעדפות כשטרם הועלו.

הכפתור **«סיכום תיק מועמדות»** מריץ את `renderCandidateSummaryModal()` הקיים (index.html:4560) על רשומת המועמדות המקושרת — אפס קוד תצוגה חדש.

### 6.3 בסיכום הסטודנט

* `renderStudentSummary()` (index.html:5088) — סקשן «▸ תיק מועמדות» בין «פרטים אישיים» ל«הכנה».
* `buildStudentSummaryText()` (index.html:5197) — בלוק טקסט מקביל, כך שהעתקה, הדפסה ומייל יכללו את הראיון.
* באזור המסמכים: שתי שורות נפרדות לקו״ח מועמדות ולקו״ח מעודכנים.

---

## 7. אפיון דף המועמדים

### 7.1 מטרות ומשימות מפתח

| מי | מה הוא צריך לעשות בדף | מדד הצלחה |
|---|---|---|
| רכז/ת | לפתוח את קורות החיים של מועמד מסוים | קליק אחד מהשורה |
| רכז/ת | לזמן ראיונות למי שמסמכיו הושלמו | מסנן ייעודי + מייל קבוצתי |
| רכז/ת | לראות מי רואיין וממתין להחלטה | צ׳יפ ייעודי במשפך |
| מרצה | לעבור על ציוני הראיון ולהחליט | ציון בשורה + סיכום בהרחבה |
| מרצה | להעביר את כל מי שעבר לסטודנטים | פעולה קבוצתית |

### 7.2 מבנה המסך

```
┌──────────────────────────────────────────────────────────────┐
│  מועמדים (27)                    [+ מועמד] [ייבוא CSV] [JSON] │
│  פרקטיקום משאבי אנוש | תשפ״ו                                  │
├──────────────────────────────────────────────────────────────┤
│  ⚡ 3 מועמדים עברו ראיון וממתינים להעברה                       │
│     [העבר את כולם]  [הצג רק אותם]                             │
├──────────────────────────────────────────────────────────────┤
│  [ממתין 8] [מסמכים 6] [ראיון נקבע 5] [רואיין 4]              │
│  [התקבל 3] [נדחה 1] [🎓 הומרו 14] [הכל 27]                   │
│  שיעור המרה 45% · זמן ממוצע מהגשה לראיון 12 ימים              │
├──────────────────────────────────────────────────────────────┤
│  [חיפוש...] [סטטוס ▾] [מסמכים ▾] [ראיון ▾] [מיון ▾] [ארכיון] │
├──────────────────────────────────────────────────────────────┤
│  ☑ בחר הכל · 3 נבחרו · [📧 מייל] [🎓 העבר] [📊 ייצא] [🗑]     │
├──────────────────────────────────────────────────────────────┤
│  ▸ שורות מועמדים                                              │
└──────────────────────────────────────────────────────────────┘
```

### 7.3 סרגל המשפך

הצ׳יפים הקיימים (index.html:4034-4040) נשמרים, בתוספת צ׳יפ **«🎓 הומרו לסטודנטים»**, שורת מדד (**שיעור המרה · זמן ממוצע מהגשה לראיון**), ולחיצה חוזרת על צ׳יפ פעיל מבטלת את הסינון.

### 7.4 סרגל סינון ומיון

| פקד | ערכים | הערות |
|---|---|---|
| חיפוש חופשי | שם, טלפון, מייל, הערות, **סיכום ראיון**, **תחום מבוקש** | קיים חלקית — להרחיב |
| סטטוס | כל הסטטוסים / 6 הסטטוסים | קיים |
| **מסמכים** | הכל / **לא מקושר במערכת** / מקושר במלואו | **חדש** — היגיינת נתונים, לא התרעת תהליך |
| **ראיון** | הכל / ללא ראיון / נקבע / השבוע / עבר תאריך ללא תוצאה / עבר / לא עבר | **חדש** |
| **מיון** | תאריך הגשה (חדש→ישן) · שם א-ב · תאריך ראיון · ציון · ותק המתנה | **חדש**, ברירת מחדל: תאריך הגשה יורד |
| **ארכיון** | מתג «הצג מומרים לסטודנטים» | **חדש**, כבוי כברירת מחדל |

המסננים נשמרים ב-`sessionStorage` כדי לשרוד רענון וניווט.

> **הערת גרסה 1.1:** מסנן «חסר CV» מגרסה 1.0 בוטל. הדרישה נאכפת לפני ההגשה, ולכן חוסר במערכת מעיד לכל היותר על רשומה שיובאה ולא קושרה — היגיינת נתונים, לא שער תהליך. השער האמיתי סביב קו״ח נמצא בדף הסטודנטים (סעיף 3.3).

### 7.5 אנטומיה של שורת מועמד

```
┌────────────────────────────────────────────────────────────────────────┐
│ ☐  דנה כהן                     [📧] [סיכום תיק] [ערוך] [🎓 העבר] [🗑] │
│    052-1234567 • dana@x.com • פתח תקווה • גיוס ומיון                   │
│                                                                         │
│    [רואיין]  [📄 קו״ח]  [📝 טופס מועמדות]  [📅 ראיון 03/02/26]         │
│    [ציון 88]  [הוגש לפני 21 ימים]                                      │
│                                                                         │
│    מחויבות ● מוטיבציה ● תקשורת ◐ אנגלית ● היכרות ○                     │
│                                                                         │
│    ▾ הצג סיכום ראיון                                                    │
│    הערות: מעדיפה ימי שני-רביעי                                          │
└────────────────────────────────────────────────────────────────────────┘
```

**שורה 1 — זיהוי ופעולות**

| רכיב | התנהגות |
|---|---|
| ☐ Checkbox | בחירה מרובה, זהה למנגנון הסטודנטים (index.html:5718) |
| שם | 15px מודגש. בארכיון מעומעם + תג «🎓 סטודנט» |
| 📧 מייל | רק אם קיים `email`; שימוש חוזר ב-`quickEmail()` |
| סיכום תיק · ערוך · מחק | קיימים |
| 🎓 העבר | **רק כש-`status === 'accepted'`**; בארכיון מוחלף ב«פתח כרטיס סטודנט» |

**שורה 2 — פרטי קשר**
`טלפון • מייל • עיר • תחום מבוקש` — טלפון ומייל כקישורי `tel:`/`mailto:`.

**שורה 3 — תגים**

| תג | מצבים | צבע |
|---|---|---|
| סטטוס | 6 הסטטוסים | לפי `CAND_STATUS_BADGE` (4021) |
| **קו״ח** | `📄 קו״ח` (קישור לתיקייה של הקובץ) / `קו״ח: לא מקושר` / `לא נדרש בקורס` / `לא רלוונטי` | ירוק / **אפור מקווקו** / אפור / אפור |
| **טופס מועמדות** | אותם 4 מצבים | ירוק / אפור מקווקו / אפור / אפור |
| ראיון | `📅 ראיון 03/02/26` / `⏰ ראיון מחר` / `⚠ עבר תאריך ללא תוצאה` | כחול / כתום / אדום |
| תוצאה | `✓ עבר` / `✗ לא עבר` | ירוק / אדום |
| **ציון** | `ציון 88` — רק אם קיים | ≥85 ירוק · 70-84 כתום · <70 אפור |
| **ותק** | `הוגש לפני N ימים` — רק אם >7 ימים ואין ראיון | כתום; >14 ימים אדום |

**התיקון המרכזי:** כל תג קובץ נגזר מ-`c.file_cv` / `c.file_application` בפועל ומפנה ל-`getDataFileFolderUrl(file.folder)` — אותה לוגיקה שכבר עובדת בשורת הסטודנט (index.html:4845-4850). המצב «לא מקושר» מוצג באפור מקווקו ולא באדום: הוא מציין שהקובץ לא צורף לרשומה במערכת, לא שהמועמד לא הגיש.

**שורה 4 — מיני-הערכה**
5 קריטריונים כנקודות: `●` גבוה (ירוק) · `◐` בינוני (כתום) · `○` נמוך (אפור). מוצג רק אם מולא לפחות אחד; `title` על כל אחד.

**שורה 5 — הרחבה**
`▾ הצג סיכום ראיון` — הטקסט המלא, קטוע ל-3 שורות עם «הצג הכל». **גישה לסיכום הראיון בלי לפתוח את הכרטיס.**

**שורה 6 — הערות**
`notes` בעיצוב הקיים; `rejectionReason` בתיבה אדומה נפרדת.

### 7.6 פעולות מרובות

| פעולה | פירוט |
|---|---|
| 📧 מייל קבוצתי | הכללה של `bulkEmailStudents()` (index.html:5762) |
| 🎓 העבר לסטודנטים | פעיל רק כשכל הנבחרים `accepted`; אחרת «N מתוך M כשירים» |
| 📅 קביעת ראיון | תאריך אחיד לנבחרים |
| 📊 ייצא נבחרים | CSV כולל השדות החדשים |
| 🗑 מחק | עם אישור וספירה |

### 7.7 מצבי קצה בתצוגה

| מצב | תצוגה |
|---|---|
| אין מועמדים | «אין מועמדים בקורס ובשנה הנוכחיים» + «+ מועמד» + «ייבוא CSV» |
| אין תוצאות לסינון | «לא נמצאו מועמדים התואמים לסינון» + «נקה סינון» |
| כולם הומרו | «כל המועמדים הומרו לסטודנטים 🎓» + «הצג ארכיון» |
| קורס בלי דרישת ראיון | תגי ראיון/ציון/הערכה לא מוצגים |

### 7.8 רספונסיביות, RTL ונגישות

* מובייל (<768px): תגים גולשים לשורות, פעולות בשורה נפרדת ברוחב מלא — לפי ה-CSS הקיים (index.html:151-205).
* כל תג צבעוני נושא גם טקסט או אייקון — לא מסתמכים על צבע בלבד.
* `title` לכל תג; `aria-label` לצ׳קבוקסים; יעד מגע 34px.

### 7.9 טבלת סטטוסים סופית

| סטטוס | תנאי | מוצג בדף המועמדים? |
|---|---|---|
| `pending` | חסרים מסמכים נדרשים | ✔ |
| `docs_ready` | כל המסמכים הנדרשים קיימים | ✔ |
| `interview_scheduled` | נקבע תאריך ראיון | ✔ |
| `interviewed` | תאריך + סיכום ראיון | ✔ |
| `accepted` | `interviewResult = 'passed'` | ✔ **רק עד ההמרה** |
| `rejected` | `interviewResult = 'failed'` | ✔ (ניתן להסתרה) |
| `converted` *(חדש)* | `converted = true` | ✘ — ארכיון בלבד |

---

## 8. שינויים נדרשים בקוד

| # | פונקציה / אזור | מיקום | סוג |
|---|---|---|---|
| 1 | `convertCandidateToStudent()` — מחליף את `transferToStudents()` | 4709 | **חדש** |
| 2 | `revertStudentToCandidate()` | — | **חדש** |
| 3 | `migrateCandidateStudentLinks()` | ליד 1454 | **חדש** |
| 4 | `getActiveCandidates()` / `getArchivedCandidates()` | 1733 | **חדש** |
| 5 | `renderCandidates()` — שכתוב התצוגה | 4023 | שכתוב |
| 6 | HTML של דף המועמדים — מסננים, באנר, סרגל פעולות | 271-300 | הרחבה |
| 7 | `saveCandidate()` — טריגר המרה | 4389 | הרחבה |
| 8 | בחירה מרובה למועמדים + 5 פעולות | ליד 5716 | **חדש** |
| 9 | **`uploadCandidateFile()` — שם קובץ `CV_מועמדות`** | 4137 | **תיקון באג** |
| 10 | **`uploadStudentFile()` — שם קובץ `CV_מעודכן` + `stage`** | 6630 | **תיקון באג** |
| 11 | **`saveStudent()` — שמירת `stage` ו-`cvUpdatedDate`** | 5001 | הרחבה |
| 12 | **מודאל סטודנט — פיצול תיבת הקו״ח לשתי משבצות** | 1004 | הרחבה |
| 13 | `renderStudents()` — תגי מועמדות, צ׳יפ שלב קו״ח, חיפוש מורחב | 4771 | הרחבה |
| 14 | `student-modal` — סקשן «תיק מועמדות» + אזהרת קו״ח | 999 | הרחבה |
| 15 | `renderStudentSummary()` + `buildStudentSummaryText()` | 5088, 5197 | הרחבה |
| 16 | `renderDashboard()` — משפך + 2 התראות | 3892 | הרחבה |
| 17 | `getCalendarEvents()` — ראיוני מומרים | 2918 | תיקון |
| 18 | `refreshReport()` — דוח מועמדים | 5887 | הרחבה |
| 19 | `exportToExcel()` / `importJSON()` | 6529, 3806 | הרחבה |
| 20 | `globalSearch()` + `showSearchResultInfo()` | 6008, 6054 | הרחבה |
| 21 | הגדרות קורס — «המרה אוטומטית» | 1168 | הרחבה |
| 22 | CSS — תגי ציון, נקודות הערכה, מצב ארכיון, צ׳יפ מקווקו | 100-205 | הרחבה |

**אין שינוי נדרש** ב-`supabaseSaveData()`.

---

## 9. תוכנית יישום מדורגת

**כל התוכנית מוקפאת בהחלטת בעל המערכת.** היא מובאת כאן מוכנה, ומחולקת לפי סיכון, כדי שאפשר יהיה להפעיל רק חלקים ממנה בעתיד.

### מסלול א' — בטוח לקישורים (אינו נוגע בקבצים או בכתובות)

| שלב | תוכן | סיכון לקישורים |
|---|---|---|
| **1** | מודל נתונים + `convertCandidateToStudent()` + מיגרציה + ארכוב | ללא — נתונים בלבד |
| **2** | תיק המועמדות בדף הסטודנט: תאריך ראיון, תוצאה, ציון, 5 קריטריונים, סיכום ראיון | ללא — טקסט בלבד |
| **3** | סינון המומרים מדף המועמדים + באנר «ממתינים להעברה» + ארכיון | ללא |
| **4** | שורת מועמד עשירה: ציון, ותק, מיני-הערכה, הרחבת סיכום ראיון, מסננים, מיון, פעולות קבוצתיות | ללא, **בתנאי** שצ׳יפי הקבצים נשארים כפי שהם היום |
| **5** | דשבורד, דוחות, לוח שנה, ייצוא/ייבוא, חיפוש גלובלי | ללא |

מסלול זה פותר את **בעיה א' במלואה** ואת **רוב בעיה ב'** — כולל את מה שנדרש במקור: סיכום הראיון והקו״ח עוברים לדף הסטודנט, והמועמדים שהומרו יורדים מהרשימה.

### מסלול ב' — נוגע בקבצים ובקישורים (מוקפא, דורש החלטה נפרדת)

| שלב | תוכן | סיכון |
|---|---|---|
| **6** | הפרדת שמות הקבצים `CV_מועמדות` / `CV_מעודכן` | **ישבור קישורי שיתוף קיימים** |
| **7** | קישור ברמת הקובץ במקום ברמת התיקייה (3.2.1) | משנה כל כתובת במערכת |
| **8** | ניהול `cvUrl` וסימון קישור מיושן | תלוי ב-6 ו-7 |

**תנאי מקדים למסלול ב':** מיפוי הקישורים שכבר נשלחו לארגונים, והחלטה מה קורה להם. בלי זה — אין לבצע.

---

## 10. קריטריוני קבלה

**הפרדת מסלולי קו״ח**

1. העלאת קו״ח בכרטיס הסטודנט **אינה** משנה את `candidacy.file_cv`, ואינה כותבת לאותו שם קובץ כמו קו״ח המועמדות.
2. שורת הסטודנט מבחינה בין «קו״ח מועמדות» ל«קו״ח מעודכנים · תאריך».
3. סטודנט שעבר הכנה וטרם העלה קו״ח מעודכנים מופיע בהתראות ובמסנן ייעודי.
4. **בתיקיית `קורות_חיים` שני הקבצים נושאים שמות מובחנים.**
5. העלאת גרסה חדשה של קו״ח מעודכנים **כן** מחליפה את הקודמת — הארגון מקבל תמיד את האחרונה.
6. **כל צ׳יפ קובץ מוביל לקובץ עצמו ולא לתיקייה** — קישור תיקייה חושף את קורות החיים של כל הסטודנטים בקורס, ואינו נשלח לארגון בשום מצב.
7. אחרי העלאת קו״ח מעודכנים, קישור שיתוף שנוצר עבור הגרסה הקודמת **מסומן כמיושן בשורה** עד שיוחלף.

**בעיה א' — הפרדה**

8. סימון «עבר ראיון» ושמירה → המועמד יורד מרשימת המועמדים ומופיע ברשימת הסטודנטים, בלחיצה אחת.
9. בכרטיס הסטודנט קיימים: קו״ח מועמדות, טופס מועמדות, תאריך ראיון, תוצאה, ציון, 5 קריטריונים וסיכום ראיון מלא.
10. סך `מועמדים פעילים + מומרים` = מספר המועמדים לפני השינוי. **אפס אובדן רשומות.**
11. המשפך מציג את המומרים ואת שיעור ההמרה.
12. ראיון של מועמד שהומר עדיין מופיע בלוח השנה.
13. המיגרציה על מסד קיים אינה יוצרת סטודנטים כפולים ואינה דורסת קו״ח קיימים.
14. «↶ ביטול» מחזיר המרה שבוצעה בטעות.

**בעיה ב' — עושר תצוגה**

15. מהשורה בלבד: פתיחת קו״ח, פתיחת טופס המועמדות, קריאת תאריך הראיון, הציון וסיכום הראיון.
16. ניתן לבחור מספר מועמדים ולבצע: מייל קבוצתי, העברה, ייצוא, מחיקה.
17. הדף תקין במובייל — אין גלישה אופקית.

---

## 11. סיכונים והפחתתם

| סיכון | הפחתה |
|---|---|
| **קו״ח מועמדות נדרסו בעבר** ולא ניתנים לשחזור | לא ניתן לתקן בקוד. התיקון מונע דריסות עתידיות; המיגרציה מסמנת רשומות שבהן `candidacy.file_cv` ו-`student.file_cv` מצביעים על אותו קובץ, לבדיקה ידנית |
| **נשלחו לארגון קו״ח לא מעודכנים** | צ׳יפ עם תאריך בשורה; סימון קישור מיושן אחרי החלפת גרסה; כפתור «העתק קישור לקו״ח מעודכנים» |
| **קישור תיקייה נשלח לארגון וחושף קו״ח של כל הסטודנטים** | קישור ברמת הקובץ בכל צ׳יפ (3.2.1); פתיחת תיקייה מופרדת לכפתור ייעודי ומסומנת «פנימי בלבד» |
| **שינוי שמות הקבצים ישבור קישורי שיתוף קיימים** | הקישור מסומן «מצביע על גרסה קודמת» עד להחלפה; אין לשנות שמות בלי מסלול הקישור (3.2.1) |
| ארגון חיצוני אינו יכול לפתוח כתובת נתיב שנבנתה במחרוזת | המערכת מובילה ישירות לקובץ; קישור השיתוף עצמו נוצר ב-SharePoint ומודבק ל-`cvUrl` |
| מיגרציה תיצור סטודנטים כפולים | התאמה מייל→טלפון→שם; `saveBeforeAction()`; דוח לפני החלה; דגל חד-פעמי |
| שני מכשירים מריצים מיגרציה במקביל | הדגל נשמר ב-snapshot; המכשיר השני מזהה ומדלג |
| משתמש יחפש מועמד שהומר ולא ימצא | חיפוש גלובלי מחזיר «מועמד (הומר)» ופותח את כרטיס הסטודנט |
| שורה עמוסה מדי | תגים מותנים בלבד; הערכה וסיכום מקופלים |
| ייבוא JSON ישן ידרוס שדות חדשים | הייבוא ממזג ולא דורס |
| שער קו״ח יחסום הזנת נתונים היסטוריים | האזהרה רכה ואינה חוסמת שמירה, בשונה משער ההכנה |

---

## נספח: השוואה לפני/אחרי

**שורת מועמד — היום**

```
דנה כהן                                    [סיכום תיק] [ערוך] [מחק]
052-1234567 • dana@x.com • פתח תקווה
[התקבל] [CV] [טופס מועמדות] [ראיון: 2026-02-03] [עבר ראיון]
```
`CV` ו`טופס מועמדות` — אותו קישור לכל המועמדים, לתיקיית הקורס, ולא לקובץ של האדם. אין ציון, אין ותק, אין סיכום ראיון, אין בחירה מרובה. והמועמדת הזו, שכבר עברה ראיון, לא אמורה להיות ברשימה בכלל.

**שורת מועמד — אחרי**

```
☐ דנה כהן                       [📧] [סיכום תיק] [ערוך] [🎓 העבר] [🗑]
  052-1234567 • dana@x.com • פתח תקווה • גיוס ומיון
  [רואיין] [📄 קו״ח] [📝 טופס מועמדות] [📅 ראיון 03/02/26] [ציון 88] [הוגש לפני 21 ימים]
  מחויבות ● מוטיבציה ● תקשורת ◐ אנגלית ● היכרות ○
  ▾ הצג סיכום ראיון
```
